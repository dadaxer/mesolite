type: dnf
repos:
    #     # files:
    #     #     - netbird.repo
    nonfree: negativo17
    cleanup: true
install:
    packages:
        - sunshine
remove:
    auto-remove: true
    packages:
        - code
        - gnome-tweaks
