type: dnf
repos:
    copr:
        - lizardbyte/beta
    #     # files:
    #     #     - netbird.repo
    # nonfree: negativo17
    cleanup: true
install:
    - sunshine
remove:
    auto-remove: true
    packages:
        - code
        - gnome-tweaks
