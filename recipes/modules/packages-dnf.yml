type: dnf
repos:
    files:
        - netbird.repo
    nonfree: negativo17
    cleanup: true
remove:
    auto-remove: true
    packages:
        - bazaar
        # - code
        - gnome-tweaks
install:
    packages:
        - blender
        - godot
        - input-remapper
        - netbird
